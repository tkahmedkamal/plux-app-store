import React from 'react';

import VerificationCodeScreen from '@/screens/auth/verification-code';

const VerificationCode = () => {
	return <VerificationCodeScreen />;
};

export default VerificationCode;
